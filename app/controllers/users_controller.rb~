class UsersController < ApplicationController

  def show
    @user = User.find(params[:id])
    @title = "#{user_name} #{@user.initials.first}. #{@user.initials.last}."
  end

  def new
    @title = t('users.sign_up.title')
  end

end
