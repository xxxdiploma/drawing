<div class = "articles" >
  <h2><%= @title %></h2>
  <% if current_user.admin? %>
  <div class = "articles button_new">
    <%= link_to t('.new'), new_article_path %>
  </div>
  <% end %>
  <hr />
  <%= render @articles %>
  <%= will_paginate %>
</div>
