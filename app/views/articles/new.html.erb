<div class = "articles new" >
  <h2><%= @title %></h2>
  <hr />
  <%= form_for(@article) do |f| %>
  <%= render 'articles/fields', :f => f %>
  <div class="actions">
    <%= f.submit t('buttons.publish') %>
  </div>
<% end %>
</div>
